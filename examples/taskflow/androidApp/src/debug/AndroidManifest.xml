<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: permit the cleartext ws:// the DevTools client uses (INTERNET itself is declared
     in the main manifest because the app loads remote images in all builds). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config" />

</manifest>
